<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    style="@style/AppCard"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="@dimen/spacing_medium"
    app:cardElevation="0dp"
    app:strokeWidth="0dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:padding="@dimen/spacing_medium"
        android:gravity="center_vertical">

        <com.google.android.material.card.MaterialCardView
            android:layout_width="52dp"
            android:layout_height="52dp"
            app:strokeWidth="0dp"
            app:cardCornerRadius="@dimen/card_corner_radius"
            app:cardBackgroundColor="@color/negro_intermedio"
            app:cardElevation="0dp">
            <ImageView
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:padding="12dp"
                android:src="@drawable/icon_payment"
                app:tint="@color/verde_claro_pixel"
                android:scaleType="fitCenter"
                android:contentDescription="Tarjeta" />
        </com.google.android.material.card.MaterialCardView>

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginStart="@dimen/spacing_medium"
            android:orientation="vertical">

            <TextView
                android:id="@+id/txtCardBrandLast4"
                style="@style/AppText.Subtitle"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                tools:text="VISA •••• 4242" />

            <TextView
                android:id="@+id/txtCardHolderExpiry"
                style="@style/AppText.Body"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="4dp"
                android:textSize="13sp"
                tools:text="Juan Pérez · Vence 08/28" />
        </LinearLayout>

        <ImageButton
            android:id="@+id/btnDeleteCard"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:background="@drawable/bg_timer_box"
            android:src="@drawable/icon_delete"
            app:tint="@color/rojo_error"
            android:padding="8dp"
            android:scaleType="fitCenter"
            android:contentDescription="Eliminar tarjeta" />

    </LinearLayout>
</com.google.android.material.card.MaterialCardView>